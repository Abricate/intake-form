import { chai, expect } from './helper';

import { diff, reduce } from '../src/util';

describe('pipedrive-webhooks', function() {
  describe('handlePipedriveDealUpdate', function() {
    it('should update job when price is set', function() {
    });

    it('should update job state', function() {
    });
  });
});
